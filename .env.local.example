# Created by Vercel CLI
# Local Development Environment Variables

# Authentication (required)
AUTH_BEARER_TOKEN=""
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# Database Configuration (required)
DATABASE_URL="postgres://user:pass@host/dbname?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://user:pass@host/dbname?sslmode=require"

# NextAuth Configuration (required)
AUTH_SECRET="" # Generate with: openssl rand -base64 32
NEXTAUTH_URL="http://app.localhost:3000"

# Domain Configuration (required)
NEXT_PUBLIC_ROOT_DOMAIN="localhost:3000"
# For local development, this should match ROOT_DOMAIN
# This is used by the middleware to handle preview deployment URLs
NEXT_PUBLIC_VERCEL_DEPLOYMENT_SUFFIX="localhost:3000"

# Database Connection Details
PGDATABASE="dbname"
PGHOST="host"
PGHOST_UNPOOLED="host-unpooled"
PGPASSWORD="password"
PGUSER="user"

# Postgres Configuration
POSTGRES_DATABASE="dbname"
POSTGRES_HOST="host"
POSTGRES_PASSWORD="password"
POSTGRES_PRISMA_URL="postgres://user:pass@host/dbname?connect_timeout=15&sslmode=require"
POSTGRES_URL="postgres://user:pass@host/dbname?sslmode=require"
POSTGRES_URL_NON_POOLING="postgres://user:pass@host/dbname?sslmode=require"
POSTGRES_URL_NO_SSL="postgres://user:pass@host/dbname"
POSTGRES_USER="user"

# Vercel Configuration
PROJECT_ID_VERCEL=""
TEAM_ID_VERCEL=""

# Optional Services
OPENAI_API_KEY=""

# Google Analytics
NEXT_PUBLIC_GA_ID=""

# Tinybird
TINYBIRD_TOKEN=""

# Local Development
NEXT_PUBLIC_VERCEL_URL="localhost:3000"

# Encryption (required for API key storage)
ENCRYPTION_KEY="" 