# Preview Environment Variables
# These variables are used for Vercel preview deployments

# Authentication (required)
AUTH_BEARER_TOKEN=""
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# Database Configuration (required)
DATABASE_URL="postgres://user:pass@host/dbname?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://user:pass@host/dbname?sslmode=require"

# NextAuth Configuration (required)
AUTH_SECRET="" # Should match across all environments


# Domain Configuration (required)
NEXT_PUBLIC_ROOT_DOMAIN="yourdomain.com"
# This is used by the middleware to handle preview deployment URLs
# Format: yourdomain.com
# Examples:
# - For custom domain: yourdomain.com
# - For Vercel domain: vercel.app
# This enables URLs like: feature-branch.yourdomain.com
NEXT_PUBLIC_VERCEL_DEPLOYMENT_SUFFIX="dereks-projects-32c37a6a.vercel.app"

# Database Connection Details
PGDATABASE="dbname"
PGHOST="host"
PGHOST_UNPOOLED="host-unpooled"
PGPASSWORD="password"
PGUSER="user"

# Postgres Configuration
POSTGRES_DATABASE="dbname"
POSTGRES_HOST="host"
POSTGRES_PASSWORD="password"
POSTGRES_PRISMA_URL="postgres://user:pass@host/dbname?connect_timeout=15&sslmode=require"
POSTGRES_URL="postgres://user:pass@host/dbname?sslmode=require"
POSTGRES_URL_NON_POOLING="postgres://user:pass@host/dbname?sslmode=require"
POSTGRES_URL_NO_SSL="postgres://user:pass@host/dbname"
POSTGRES_USER="user"

# Vercel Configuration
PROJECT_ID_VERCEL=""
TEAM_ID_VERCEL=""

# Optional Services
OPENAI_API_KEY=""

# Google Analytics
NEXT_PUBLIC_GA_ID=""

# Tinybird
TINYBIRD_TOKEN="" 