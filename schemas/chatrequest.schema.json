{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ChatRequest",
  "description": "Defines the structure for chat requests between the frontend and API",
  "type": "object",
  "required": ["message"],
  "properties": {
    "message": {
      "type": "string",
      "description": "The message text from the user"
    },
    "stream": {
      "type": "boolean",
      "description": "Optional flag to indicate if streaming should be enabled",
      "default": true
    }
  },
  "additionalProperties": false
} 